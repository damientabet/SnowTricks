{% extends 'base.html.twig' %}

{% block body %}
    <div class="d-flex justify-content-center vh-100 w-100" style="margin-top: 110px">
        <div class="col-lg-9">
            {{ form_start(form) }}
            {{ form_label(form.content) }}
            {{ form_widget(form.content, {'attr': {'class': 'mt-3 mb-3 p-3 rounded-0'}}) }}
            {{ form_end(form) }}
            <a class="btn btn-outline-success rounded-0" href="{{ path('trick.show', {'id': idTrick.id, 'slug': slug}) }}">Retour à la figure</a>
        </div>
    </div>
{% endblock %}